package mvc;

public class Model {
	
	// Cette fonction reçoi du texte la converti en majuscule et la renvoi.
	// C'est trés simple mai dans notre cas cela nous permettra de representer un calcul
	public String convertiEnMajuscule(String texte) {
		return texte.toUpperCase();
	}
}
